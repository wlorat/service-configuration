company:
  property: User Profile Dev Update

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/one-test
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
  properties:
    hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect
  show-sql: true